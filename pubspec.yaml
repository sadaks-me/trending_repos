name: go_jek
description: A GoJek Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  provider: ^3.0.0+1
  connectivity: ^0.4.3+2
  async_resource: ^0.1.4
  path_provider: ^1.1.0
  shimmer: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
   - assets/images/
   - assets/icons/